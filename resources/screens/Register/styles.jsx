import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  rootContainer: {
    flex: 1,
  },
  container: {
    margin: 'auto',
    padding: 8,
    width: '100%',
    gap: 10,
    justifyContent: 'center',
  },
  registerLabel: { textAlign: 'center', marginTop: 20 },
  loginBtn: {
    marginTop: 10,
  },
});

export default styles;
